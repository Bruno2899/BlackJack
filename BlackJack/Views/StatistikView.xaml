<UserControl x:Class="BlackJack.Views.StatistikView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <StackPanel Margin="20">

        <TextBlock Text="Statistik"
                   FontSize="24"
                   Margin="0,0,0,20"/>

        <TextBlock Text="{Binding SpieleAnzahl, StringFormat=Spiele: {0}}"
                   Margin="0,5"/>

        <TextBlock Text="{Binding GesamtGewinn, StringFormat=Gesamtgewinn: {0:F1}}"
                   Margin="0,5"/>

        <TextBlock Text="{Binding WinRate, StringFormat=Win Rate: {0:F1}%}"
                   Margin="0,5"/>

        <TextBlock Text="{Binding DurchschnittGewinn, StringFormat=Durchschnitt: {0:F2}}"
                   Margin="0,5"/>

        <TextBlock Text="{Binding GroessterGewinn, StringFormat=Größter Gewinn: {0:F1}}"
                   Margin="0,5"/>

        <Button Content="Zurück"
                Width="200"
                Height="50"
                Margin="0,20,0,0"
                Command="{Binding BackCommand}"/>

    </StackPanel>

</UserControl>