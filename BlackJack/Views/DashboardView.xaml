<UserControl x:Class="BlackJack.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Black">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Center">

            <TextBlock Text="BLACKJACK"
                       FontSize="48"
                       FontWeight="Bold"
                       Foreground="Gold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,0"/>
            <TextBlock Text=".exe"
           FontSize="18"
           Foreground="White"
           HorizontalAlignment="Center"
           Margin="0,0,0,30"/>

            <TextBlock Text="Dashboard"
                       FontSize="24"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,40"/>

            <Button Content="Spiel Starten"
                    Width="250"
                    Height="60"
                    FontSize="18"
                    Margin="0,0,0,15"
                    Command="{Binding StartGameCommand}"/>

            <Button Content="Statistik"
                    Width="250"
                    Height="60"
                    FontSize="18"
                    Margin="0,0,0,15"
                    Command="{Binding StatistikCommand}"/>
            <Button Content="Logout"
        Width="150"
        Height="30"
        Margin="0,0,0,15"
        Command="{Binding LogoutCommand}"/>

        </StackPanel>

    </Grid>

</UserControl>